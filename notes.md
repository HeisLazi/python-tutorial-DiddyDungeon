idk just to dot down notes on stuff id like to remember

- DIRECTIONS GRILL 1.
  1. a task should remember what exactly.
        - it should have how important it is (we can use numbers cause everyone always uses high/medium/low)
        - it should have a deadline
        - it should have a title
        - it should have a mini brief  for description

    2. how can the program tell when a task is completed
        - we should have a progress bar that shows how much of the task is completed
        - we should have checkpoints for tasks that are like multiphase or multiple steps
        - and for smaller tasks that we cant track like irl ones we just have a tick box

    3. can a task be completed more than once
        -  for some types of tasks having them be recurring tasks would be good yea like daily or weekly tasks

    4. what should happen if a task has an empty name
        - it doesnt get added and throws and error message

    5. what should the player see when listing tasks
        - it should show the properties of the task like title, importance, deadline, and progress


v1 properties will only be title, description, importance, completed selection (yes/no).
