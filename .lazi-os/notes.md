# python-tutorial-DiddyDungeon
Purpose: Python Quest Lab is Lazi's learn-by-building Python campaign, with PYR acting as a Socratic tutor instead of an implementation bot. It serves skill acquisition: Lazi writes, debugs and explains the code while the game shell, Campaign, Practice, Codex and Dungeon make repetition engaging.
Current state: Local roadmap slices 0–6 are implemented and verified: canonical progression/state service, Campaign encounters, boss/project progression contracts, Codex, Homestead/economy, playable Infinite Dungeon, shared `tutor.py` Practice history and runtime diagnostics; the 2026-09-16 checkpoint recorded 78/78 backend tests, 36/36 frontend checks, a passing Vite build and live shell/AI PTYs.
In progress: Friend-ready distribution remains open in `ROADMAP_EXECUTION_PLAN.md`: guarded Windows/native-Linux launch, real CachyOS F-058 proof, save-custody/two-device acceptance and later desktop packaging; `tools/questlab-launch.ps1` and the launcher/health path are foundations, while hosted Supabase/Tauri/social work remains gated.
Next up: 1) prove the native CachyOS target on the real laptop with one canonical state path and both PTYs alive; 2) complete friend-ready Windows/local distribution and save-custody gates; 3) only start hosted/Tauri/social stages after their explicit approval gates.
Gotchas: `LocalStateService` is the only progression writer and repo-root `progress.json` is canonical; Practice may write controlled tutor/notes data but must never mutate Campaign/Dungeon rewards, HP, Resolve or combat; hidden future questions/answers must never reach the browser; browser acceptance is keyboard/mouse driven and Playwright is prohibited for those K&M gates.
Do not touch: Canonical `progress.json` directly, protected user `tutor.py` work, state-event/revision invariants, connected PTYs, or provider/hosted state without the required isolated-state and approval gates.