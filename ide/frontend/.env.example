# Quest Lab remains fully usable when both values are empty or omitted.
# These VITE_ values are the public browser client configuration, not secrets.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Never place a Supabase service-role key, AI credential, or CLI token here.
